---
description: Activate caveman mode with specified intensity level
trigger: /caveman
---

Read the skill file at ~/.config/opencode/skills/caveman/SKILL.md and activate caveman communication mode.

INTENSITY LEVEL:
- If user says "lite" → lite mode (no filler, keep grammar)
- If user says "ultra" → ultra mode (maximum compression)
- If user says "wenyan" → classical Chinese mode
- Otherwise → full mode (default caveman)

Respond acknowledging the mode activation in the requested style.
