# agent-configuration Specification

## Purpose

Requirements for OpenCode agent configuration, specifically the agent definitions
and prompt content in `~/.config/opencode/opencode.json`. This covers fallback
skill loading instructions, agent prompt structure, and configuration validation
rules.

> NOTE: This is a new domain created by change `sdd-bugfix-skill-registry-orchestrator-terminology`.
> The full requirements are defined in the delta spec at
> `openspec/changes/sdd-bugfix-skill-registry-orchestrator-terminology/specs/agent-configuration/spec.md`.
> This main spec will be populated when the change is archived.

## Requirements

_Requirements to be populated from delta spec upon archive._

### Placeholder

This file exists to establish the domain. Full requirements will be merged
during `sdd-archive`.
