# Tasks: T14 HDM Migration v2

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~350 (5 new ~200, 3 modified ~100 net, 1 deleted -71, 1 doc ~30) |
| 400-line budget risk | Low |
| Chained PRs recommended | No |
| Delivery strategy | single-pr |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: Low

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | HDM v2 migration (infra + configs + cleanup + verify) | single PR to master | size-exception: 5 new files, 1 deleted, 3 modified |

## Phase 1: Infrastructure (2 tasks)

- [ ] 1.1 Add `hyprdynamicmonitors` flake input to `flake.nix` (after `nixos-hardware`, line ~33) with `inputs.nixpkgs.follows = "nixpkgs"`
- [ ] 1.2 Import `inputs.hyprdynamicmonitors.homeManagerModules.default` in `hosts/t14/default.nix` `home-manager.users.glats.imports`; add UPower assertion (`config.services.upower.enable`)

## Phase 2: HDM Config Files (6 tasks)

- [ ] 2.1 Create `hosts/t14/hdm/config.toml` — 4 profiles (docked_lid_open, docked_lid_closed, undocked_lid_open, undocked_lid_closed) using `description` (EDID) matching, `[fallback_profile]`, `[scoring]` (lid_state_match=10, monitor_match=5, description_match=5), `[lid_events]` (enabled=true), `[general]` (debounce_time_ms=1500)
- [ ] 2.2 Create `hosts/t14/hdm/hyprconfigs/docked-lid-open.conf` — eDP-1 at `4920x420`, 3 externals at y=420 (AOC 24P1W1 with `transform,1`), workspace rules for ws 1-20 (1-3 on eDP-1, 4-20 distributed across externals via `monitor:desc:...`)
- [ ] 2.3 Create `hosts/t14/hdm/hyprconfigs/docked-lid-closed.conf` — eDP-1 parked at `-30000x0` (NOT disabled), 3 externals at y=0, workspace rules for ws 4-20 only (no ws 1-3)
- [ ] 2.4 Create `hosts/t14/hdm/hyprconfigs/undocked-lid-open.conf` — eDP-1 at `0x0`, workspace rules for ws 1-3 only
- [ ] 2.5 Create `hosts/t14/hdm/hyprconfigs/undocked-lid-closed.conf` — eDP-1 at `0x0` (clamshell pre-suspend), ws 1-3 only
- [ ] 2.6 Create `hosts/t14/hdm/hyprconfigs/fallback.conf` — generic `monitor=,preferred,auto,1`

## Phase 3: Cleanup Old Stack (4 tasks)

- [ ] 3.1 Modify `hosts/t14/home/default.nix` — REMOVE `seedHyprSettings` activation, `udev-settle.conf` drop-in, `monitor-lid-validator` systemd unit, validator `home.file`; ADD `home.hyprdynamicmonitors` block (package from flake input, `config = builtins.readFile ./hdm/config.toml`, `extraFiles` for 5 hyprconfigs, `installExamples = false`, `extraFlags = ["--enable-lid-events"]`)
- [ ] 3.2 Modify `hosts/t14/home/hypr/monitors.nix` — REMOVE entire `extraConfig` block, `mkWorkspaceRules` let-binding, `workspace = mkWorkspaceRules`; ADD `extraConfig = "source = ${config.home.homeDirectory}/.config/hypr/config.d/99_autogenerated-monitors.conf"`; KEEP `env = ["GDK_SCALE,1"]`
- [ ] 3.3 Delete `hosts/t14/home/scripts/monitor-lid-validator.sh`
- [ ] 3.4 Update `docs/t14-monitor-layout.md` — replace daemon/validator architecture description with HDM event-driven model; remove `ENABLE_LAPTOP` references

## Phase 4: Verification (3 tasks)

- [ ] 4.1 Run `format-nix` on all changed .nix files
- [ ] 4.2 Run `nix flake check --no-build` — must pass for t14 (verifies eval + module wiring)
- [ ] 4.3 Grep gate: `grep -r "monitor-lid-validator\|settings.conf\|udev-settle\|ENABLE_LAPTOP\|seedHyprSettings" hosts/t14/` returns empty
