# Tasks: T14 HDM Migration v3

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~350 |
| 400-line budget risk | Low |
| Chained PRs recommended | No |
| Suggested split | Single commit |
| Delivery strategy | exception-ok |
| Chain strategy | size-exception |

Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: size-exception
400-line budget risk: Low

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Full HDM migration | PR 1 → master | Under 400-line budget; user requested single commit for easy rollback |

## Phase 1: Infrastructure

- [x] 1.1 Add `hyprdynamicmonitors` flake input to `flake.nix`: `url = "github:fiffeek/hyprdynamicmonitors"`, `inputs.nixpkgs.follows = "nixpkgs"`, `inputs.home-manager.follows = "home-manager"`
- [x] 1.2 Import `inputs.hyprdynamicmonitors.homeManagerModules.default` in `hosts/t14/default.nix` under `home-manager.users.glats.imports`
- [x] 1.3 Add UPower assertion in `hosts/t14/default.nix`: `assertion = config.services.upower.enable` with message referencing `modules/hardware/amd-laptop.nix`

## Phase 2: HDM Config Files

- [x] 2.1 Create `hosts/t14/hdm/config.toml`: 4 profiles (`docked_lid_open`, `docked_lid_closed`, `undocked_lid_open`, `undocked_lid_closed`) each with `required_monitors` (EDID description list), `required_lid_states`, `config_file` + `config_file_type = "static"`; `[fallback_profile]`; `[scoring]` (lid_state_match=10, monitor_match=5, description_match=5); `[lid_events]` (enabled=true); `[general]` (debounce_time_ms=1500); `match_description_using_regex = true`
- [x] 2.2 Create `hosts/t14/hdm/hyprconfigs/docked-lid-open.conf`: `monitor=desc:` for eDP-1 at `4920x420` + 3 externals at y=420 (AOC 24P1W1 with `transform,1`); `workspace=` rules: ws 1-3 on eDP-1 `monitor:desc:`, ws 4-20 mod-3 across 3 externals
- [x] 2.3 Create `hosts/t14/hdm/hyprconfigs/docked-lid-closed.conf`: `monitor=desc:` for eDP-1 at `-30000x0`, externals at y=0 (AOC 24P1W1 `transform,1`); `workspace=` rules: ws 4-20 mod-3 only (no ws 1-3 — eDP-1 off-screen)
- [x] 2.4 Create `hosts/t14/hdm/hyprconfigs/undocked-lid-open.conf`: `monitor=desc:` for eDP-1 at `0x0`; `workspace=` rules: ws 1-3 only
- [x] 2.5 Create `hosts/t14/hdm/hyprconfigs/undocked-lid-closed.conf`: same as `undocked-lid-open.conf` — eDP-1 at `0x0`, ws 1-3 only (clamshell pre-suspend)
- [x] 2.6 Create `hosts/t14/hdm/hyprconfigs/fallback.conf`: `monitor=,preferred,auto,1` — generic fallback, no workspace rules

## Phase 3: Cleanup Old Stack

- [x] 3.1 Modify `hosts/t14/home/default.nix`: ADD `home.hyprdynamicmonitors` block (`enable=true`, `configFile = ./hdm/config.toml`, `extraFiles = { "hyprdynamicmonitors/hyprconfigs" = ./hdm/hyprconfigs; }`, `extraFlags = [ "--enable-lid-events" ]`); REMOVE `home.activation.seedHyprSettings`, `xdg.configFile` udev-settle drop-in, `systemd.user.services.monitor-lid-validator`, `home.file` validator script
- [x] 3.2 Modify `hosts/t14/home/hypr/monitors.nix`: REMOVE `mkWorkspaceRules` let-binding, `workspace = mkWorkspaceRules`, ALL `extraConfig` (both `bindl` blocks, both hyprlang `if/endif` blocks, `source` to settings.conf); ADD `source = config.home.homeDirectory + "/.config/hypr/config.d/99_autogenerated-monitors.conf"`; KEEP `env = [GDK_SCALE, ...]`
- [x] 3.3 Delete `hosts/t14/home/scripts/monitor-lid-validator.sh`
- [x] 3.4 Update `docs/t14-monitor-layout.md`: document HDM architecture, profile-driven layout, UPower lid events; remove ENABLE_LAPTOP, daemon, settings.conf, and udev-settle references

## Phase 4: Verification

- [x] 4.1 Run `format-nix` on all changed .nix files
- [x] 4.2 Run `nix flake check --no-build` — must pass for t14 (no cross-host regressions)
- [x] 4.3 Grep gate: `grep -r "monitor-lid-validator\|settings.conf\|udev-settle\|ENABLE_LAPTOP\|seedHyprSettings\|mkWorkspaceRules" hosts/t14/` returns empty
