# Tasks: HyprDynamicMonitors Migration (t14)

## Review Workload Forecast

Decision needed before apply: Yes
Chained PRs recommended: No
Chain strategy: pending
400-line budget risk: Low

## Phase 1: Wire HDM Module (commit 1)

- [ ] 1.1 `flake.nix`: add input `hyprdynamicmonitors = { url = "github:fiffeek/hyprdynamicmonitors"; inputs.nixpkgs.follows = "nixpkgs"; };`
- [ ] 1.2 `hosts/t14/default.nix`: append `inputs.hyprdynamicmonitors.homeManagerModules.default` to `home-manager.users.glats.imports`. **Do not touch `omarchy = { ... }` block** — keeps `omarchy.hyprland.lidSwitch.enable = false` intact
- [ ] 1.3 `nix flake check --no-build` — exits 0; no new services

## Phase 2: TOML Profiles (commit 2)

- [ ] 2.1 `hosts/t14/hdm/config.toml` — `[general]` (destination `~/.config/hypr/config.d/99_autogenerated-monitors.conf`, `debounce_time_ms = 1500`), `[lid_events]`, `[scoring]` (10/5/2), 4 profile blocks + fallback. eDP-1 uses `name = "eDP-1"` + `monitor_tag = "laptop"`
- [ ] 2.2 `hosts/t14/hdm/hyprconfigs/docked-lid-open.conf` — eDP-1 at `preferred,4920x420,1`; externals at y=420 (AOC 24P1W1 with `,transform,1`)
- [ ] 2.3 `hosts/t14/hdm/hyprconfigs/docked-lid-closed.conf` — eDP-1 at `preferred,-30000x0,1` (park, NOT disable); externals at y=0
- [ ] 2.4 `hosts/t14/hdm/hyprconfigs/undocked-lid-open.conf` — `monitor=eDP-1,preferred,0x0,1`
- [ ] 2.5 `hosts/t14/hdm/hyprconfigs/fallback.conf` — `monitor=,preferred,auto,1`
- [ ] 2.6 `nix flake check --no-build` — exits 0

## Phase 3: Enable HDM + Strip Old Monitors (commit 3)

- [ ] 3.1 `hosts/t14/home/default.nix`: add `home.hyprdynamicmonitors = { enable = true; configFile = ./hdm/config.toml; extraFiles."hyprdynamicmonitors/hyprconfigs" = ./hdm/hyprconfigs; extraFlags = [ "--enable-lid-events" ]; };`. Keep validator/activation/udev for now
- [ ] 3.2 `hosts/t14/home/hypr/monitors.nix`: strip `extraConfig` — remove `source = .../settings.conf`, `# hyprlang if` blocks, `monitor=` lines, `bindl` lines
- [ ] 3.3 `hosts/t14/home/hypr/monitors.nix`: replace stripped `extraConfig` with `source = ~/.config/hypr/config.d/99_autogenerated-monitors.conf`. Keep `mkWorkspaceRules` + `env = [ "GDK_SCALE,1" ]` in `settings`
- [ ] 3.4 `nix flake check --no-build` — exits 0; HDM service registered

## Phase 4: Cleanup Old Validator (commit 4)

- [ ] 4.1 `hosts/t14/home/default.nix`: remove `home.activation.seedHyprSettings` (lines 33-38)
- [ ] 4.2 `hosts/t14/home/default.nix`: remove `xdg.configFile.".../udev-settle.conf"` (lines 42-45)
- [ ] 4.3 `hosts/t14/home/default.nix`: remove `systemd.user.services.monitor-lid-validator` (lines 50-67)
- [ ] 4.4 `hosts/t14/home/default.nix`: remove `home.file.".local/bin/monitor-lid-validator.sh"` (lines 78-81)
- [ ] 4.5 Delete `hosts/t14/home/scripts/monitor-lid-validator.sh`
- [ ] 4.6 `nix flake check --no-build` — exits 0

## Phase 5: Verify (commit 5)

- [ ] 5.1 `nix flake check --no-build` — all 3 toplevels build
- [ ] 5.2 `format-nix` — format all modified .nix files
- [ ] 5.3 `grep -rE "monitor-lid-validator|seedHyprSettings|udev-settle|ENABLE_LAPTOP" hosts/t14/` — zero matches
- [ ] 5.4 `ls hosts/t14/hdm/hyprconfigs/` — exactly 4 .conf files
- [ ] 5.5 Manual gate (post-merge): `systemctl --user status hyprdynamicmonitors` active; `hyprdynamicmonitors run --dry-run` selects correct profile

## Acceptance Criteria

- **Phase 1**: `nix flake check` exits 0; `omarchy = { ... }` block byte-identical; HDM module imported but no options set
- **Phase 2**: 5 new files under `hosts/t14/hdm/`; zero existing files modified; `nix flake check` exits 0
- **Phase 3**: `monitors.nix` contains only `mkWorkspaceRules`, `env`, and one `source` line; old validator still running in parallel (safe — `omarchy.hyprland.lidSwitch.enable = false`); `nix flake check` exits 0
- **Phase 4**: `monitor-lid-validator.sh` deleted; service/activation/udev drop-in all removed; `nix flake check` exits 0
- **Phase 5**: All grep checks pass; build gate passes; manual test matrix succeeds (boot docked+lid-open, close lid while docked, undock with lid open)

## Snippets

`monitors.nix` after Phase 3:
```nix
extraConfig = ''
  source = ~/.config/hypr/config.d/99_autogenerated-monitors.conf
'';
```
